{
  "name": "@clerc/monorepo",
  "private": true,
  "version": "0.40.0",
  "packageManager": "pnpm@8.6.9",
  "author": "Ray <i@mk1.io> (https://github.com/so1ve)",
  "description": "",
  "keywords": [],
  "homepage": "https://github.com/so1ve/clerc#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/so1ve/clerc.git"
  },
  "bugs": {
    "url": "https://github.com/so1ve/clerc/issues"
  },
  "license": "MIT",
  "scripts": {
    "build": "rimraf packages/*/dist && pnpm -r run build",
    "lint": "eslint . && prettier . --check",
    "lint:fix": "eslint . --fix && prettier . --write",
    "prepublishOnly": "nr build",
    "release": "bumpp package.json packages/*/package.json --commit --push --tag && nr build && pnpm publish -r --filter ./packages/*",
    "test": "vitest",
    "typecheck": "tsc --noEmit",
    "watch": "pnpm -r --parallel run watch"
  },
  "devDependencies": {
    "@antfu/ni": "^0.21.4",
    "@so1ve/eslint-config": "^0.114.2",
    "@so1ve/prettier-config": "^0.114.2",
    "@types/node": "^18.16.18",
    "bumpp": "^9.1.1",
    "eslint": "^8.43.0",
    "pkgroll": "^1.10.0",
    "prettier": "^3.0.0",
    "rimraf": "^3.0.2",
    "typescript": "^5.1.3",
    "vite": "^4.3.9",
    "vitest": "^0.32.2"
  }
}
